import FormFile from '../../components/UI/FormFile/FormFile';

const ImageConvertor = () => {
  return <FormFile title='конвертировать в png' titleInput="Выбрать файл" />;
};

export default ImageConvertor;
